# Vercel Python runtime installs from this file (pyproject stays the source
# of truth for local dev: pip install -e ".[dev]").
fastapi>=0.110
sqlalchemy>=2.0
pydantic>=2.6
anthropic>=0.90
yfinance>=0.2.40
pandas>=2.0
# explicit: engine/montecarlo.py and research/stats.py import numpy directly.
# It arrives transitively via pandas today, which is not a guarantee.
numpy>=1.26
# Split deliberately. `psycopg[binary]` uses extras-bracket syntax, and Vercel's
# Python builder resolved every other package while silently skipping this one —
# the deployment died at import with ModuleNotFoundError: No module named
# 'psycopg' while the requirement sat right here in the file. Naming the binary
# wheel as its own top-level requirement removes the extras resolution entirely.
psycopg>=3.1
psycopg-binary>=3.1
